// Structure interface
export interface Structure {
  id: number;
  name: string;
  content: string;
}

// FolderStructure interface
export interface FolderStructure {
  // Define the structure here if needed
}
